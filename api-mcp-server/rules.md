# API MCP Server Rules

- Smoke output must include route, render type, pass/fail, job ID when available, and output URL when available.
- After API contract changes, sync API clients and update documentation.
