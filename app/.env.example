SYSTEM_SECRET=dev-admin-secret
ENCRYPTION_KEY=dev-encryption-key-change-me
DATABASE_CONNECTION_STRING=postgresql://vgen:vgen@localhost:5432/vgen?sslmode=require
DATABASE_SSL=require

# Optional: paths to ffmpeg/ffprobe. Defaults to "ffmpeg"/"ffprobe" on PATH.
# FFMPEG_PATH=ffmpeg
# FFPROBE_PATH=ffprobe
