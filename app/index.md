# App

Fastify API, application use cases, Prisma context, infrastructure adapters, Remotion templates, and background workers live here.

Routes call use cases. Use cases may use Prisma context directly. Infrastructure implements shared-kernel contracts.
