# Settings Rules

- API files must use settings use cases.
- Sensitive settings must be masked before returning to UI.
- JSON writes must be atomic through the settings writer.
- One DTO, one use case, and one endpoint per file.
