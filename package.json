{
  "name": "vgen-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter app --filter ui dev",
    "dev:app": "pnpm --filter app dev",
    "dev:ui": "pnpm --filter ui dev",
    "db:migrate": "pnpm --filter app db:migrate"
  }
}
